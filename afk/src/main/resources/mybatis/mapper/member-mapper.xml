<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">
	<insert id="insertMember" parameterType="Member">
		INSERT INTO MEMBER(mb_id,mb_pwd,mb_name,mb_email,mb_phone,mb_joindate) VALUES (#{mb_id}, #{mb_pwd}, #{mb_name},#{mb_email},#{mb_phone},sysdate)
	</insert>
	<select id="confirmUserId" parameterType="Member" resultType="Integer">
		SELECT COUNT(*) FROM MEMBER WHERE MB_ID = #{mb_id}
	</select>
</mapper>